<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เพิ่มการ์ด</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/add-card-test-styles.css">
    <link rel="website icon" type="png" href="images/logo.png">
</head>

<body>
    <script src="/js/opennav-script.js"></script>
    <header>
        <div class="header">
            <div class="container">
                <button class="openbtn" onclick="openNav()">☰</button>
                <div class="logo_section">
                    <div class="logo">
                        <a href="#"><img src="images/logo.png" alt="#"></a>
                    </div>
                </div>
                <div class="menu-area">
                    <nav class="main-menu">
                        <ul class="menu-area-main">
                            <li><a href="/">Home</a></li>
                            <li><a href="/characters">ตัวอักษร</a></li>
                            <li><a href="/lessons">บทเรียน</a></li>
                            <li><a href="/tests">Quiz</a></li>
                        </ul>
                    </nav>
                </div>
                <div class="menu-right">
                    <li class="dropdown">
                        <a href="#" class="dropbtn"><% if (user) { %><%= user.username %><% } else { %>Account<% } %></a>
                        <div class="dropdown-content">
                            <% if (user) { %>
                                <a href="/profile">Profile</a>
                                <a href="/logout">ออกจากระบบ</a>
                            <% } else { %>
                                <a id="bt-link" href="/login">Log In</a>
                                <a id="bt-link" href="/sign_up">Sign Up</a>
                            <% } %>
                        </div>
                    </li>
                </div>
            </div>
        </div>
        
        <div id="mySidebar" class="sidebar">
            <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">×</a>
            <a href="/profile">My profile</a>
        </div>
    </header>


    <div class="form-container">
        <h1>เพิ่มการ์ด</h1>
        <form action="/add-card" method="POST" enctype="multipart/form-data">
            <label for="card_name">ชื่อการ์ด:</label>
            <input type="text" id="card_name" name="card_name" required>

            <label for="card_description">คำอธิบาย:</label>
            <textarea id="card_description" name="card_description" required></textarea>

            <label for="card_image">รูปภาพการ์ด:</label>
            <input type="file" id="card_image" name="card_image" accept="image/*">

            <h2>เพิ่มคำศัพท์</h2>
            <div id="vocab-container">
                <div class="vocab">
                    <label for="japanese_word_0">คำญี่ปุ่น:</label>
                    <input type="text" id="japanese_word_0" placeholder="คำญี่ปุ่น" name="vocab[0][japanese_word]"
                        required>

                    <label for="reading_0">การอ่าน:</label>
                    <input type="text" id="reading_0" placeholder="การอ่าน" name="vocab[0][reading]" required>

                    <label for="translation_0">การแปล:</label>
                    <input type="text" id="translation_0" placeholder="การแปล" name="vocab[0][translation]" required>

                    <label for="vocab_image_0">รูปภาพคำศัพท์:</label>
                    <input type="file" id="vocab_image_0" name="vocab[0][image]" accept="image/*">
                </div>
            </div>

            <button type="button" id="addVocab" onclick="addVocab()">เพิ่มคำศัพท์เพิ่มเติม</button>

            <br><br>

            <button type="submit" id="submit">บันทึก</button>
        </form>
    </div>

    <script>
        let vocabIndex = 1;
        function addVocab() {
            const vocabContainer = document.getElementById("vocab-container");
            const newVocab = document.createElement("div");
            newVocab.classList.add("vocab");
            newVocab.innerHTML = `
        <label for="japanese_word_${vocabIndex}">คำญี่ปุ่น:</label>
        <input type="text" id="japanese_word_${vocabIndex}" placeholder="คำญี่ปุ่น" name="vocab[${vocabIndex}][japanese_word]" required>
    
        <label for="reading_${vocabIndex}">การอ่าน:</label>
        <input type="text" id="reading_${vocabIndex}" placeholder="การอ่าน" name="vocab[${vocabIndex}][reading]" required>
    
        <label for="translation_${vocabIndex}">การแปล:</label>
        <input type="text" id="translation_${vocabIndex}" placeholder="การแปล" name="vocab[${vocabIndex}][translation]" required>
    
        <label for="vocab_image_${vocabIndex}">รูปภาพคำศัพท์:</label>
        <input type="file" id="vocab_image_${vocabIndex}" name="vocab[${vocabIndex}][image]" accept="image/*">
    `;

            vocabContainer.appendChild(newVocab);
            vocabIndex++;
        }
        document.getElementById('addVocab').addEventListener('click', addVocab);

    </script>

</body>

</html>