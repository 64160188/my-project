<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Lessons</title>
    <link rel="stylesheet" href="/css/test-styles.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" type="image/png" href="images/logo.png">
</head>

<body>
    <script src="/js/opennav-script.js"></script>
    <header>
        <div class="header">
            <div class="container">
                <button class="openbtn" onclick="openNav()">☰</button>
                <div class="logo_section">
                    <div class="logo">
                        <a href="/"><img src="images/logo.png" alt="#"></a>
                    </div>
                </div>
                <div class="menu-area">
                    <nav class="main-menu">
                        <ul class="menu-area-main">
                            <li><a href="/">Home</a></li>
                            <li><a href="/characters">ตัวอักษร</a></li>
                            <li><a href="/lessons">บทเรียน</a></li>
                            <li><a href="/tests">Quiz</a></li>
                        </ul>
                    </nav>
                </div>
                <div class="menu-right">
                    <li class="dropdown">
                        <a href="#" class="dropbtn"><% if (user) { %><%= user.username %><% } else { %>Account<% } %></a>
                        <div class="dropdown-content">
                            <% if (user) { %>
                                <a href="/profile">Profile</a>
                                <a href="/logout">ออกจากระบบ</a>
                            <% } else { %>
                                <a id="bt-link" href="/login">Log In</a>
                                <a id="bt-link" href="/sign_up">Sign Up</a>
                            <% } %>
                        </div>
                    </li>
                </div>
            </div>
        </div>
        
        <div id="mySidebar" class="sidebar">
            <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">×</a>
            <a href="/profile">My profile</a>
        </div>
    </header>


    <main class="container">
        <div class="tabs">
            <div class="dropdown">
                <button class="dropbtn-add">Add</button>
                <button class="tab-button" onclick="filterCategory('all')">ทั้งหมด</button>
                <button class="tab-button" onclick="filterCategory('card')">Vocab Matching Game</button>
                <button class="tab-button" onclick="filterCategory('quiz')">บททดสอบ</button>
                <!-- <input type="text" id="searchBox" placeholder="Search for items..."> -->
                <div class="dropdown-content">
                    <a href="/add-test">Add Test</a>
                    <a href="/add-card">Add card</a>
                </div>
            </div>
        </div>

        <section class="grid" id="lessonGrid">
            <% tests.forEach(test => { %> 
                <a href="/tests/<%= test.test_id %>" class="card-link">
                    <article class="lesson-item" data-category="<%= test.category %>">
                        <h2 class="title"><%= test.name %></h2>
                        <img src="/uploads/<%= test.image %>" alt="Test Image">
                        <p class="destipt"><%= test.description %></p>
                    </article>
                </a>
            <% }); %>
        </section>
        
        <h1>Cards</h1>
        <section class="grid" id="cards-section">
            <% if (cards && cards.length > 0) { %>
                <% cards.forEach(card => { %>
                    <a href="/card/<%= card.card_id %>" class="detail-button">
                        <div class="card-item">
                            <h2 class="title"><%= card.card_name %></h3>
                            <img src="/uploads/<%= card.image %>" alt="Card Image" class="card-image">
                            <p class="destipt"><%= card.card_description %></p><br>
                        </div>
                    </a>
                <% }); %>
            <% } else { %>
                <p>No cards available.</p>
            <% } %>
        </section>
    </main>

    <script src="/js/addTest-script.js"></script>



    <footer>
        <div class="headinga">
            <h3>เรียนภาษาญี่ปุ่น กับ ChanPe</h3>
            <span>Burapha University, 169 Long Had Bangsaen Rd ,Thailand, TH</span>
            <p>(+66) 8866458**
                <br>64160188@go.buu.ac.th
            </p>
        </div>
    </footer>
</body>

</html>
